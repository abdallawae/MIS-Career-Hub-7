const $ = (id) => document.getElementById(id);
const msg = $('authMsg');
const params = new URLSearchParams(location.search);
const next = params.get('next') || 'account.html';
const PRODUCTION_AUTH_URL = 'https://abdallawae.github.io/MIS-Career-Hub-7/auth.html';
const PRODUCTION_SITE_URL = 'https://abdallawae.github.io/MIS-Career-Hub-7/';
function safeNext(value){ if(!value || value.startsWith('http://') || value.startsWith('https://') || value.startsWith('//')) return 'account.html'; return value; }
const destination = safeNext(next);
function notice(text, ok=false){ if(msg){ msg.textContent=text; msg.className='notice' + (ok?' success':''); } }
function showRecoveryMode(){ if($('recoveryBox')) $('recoveryBox').hidden=false; if($('loginArea')) $('loginArea').hidden=true; notice('أنت في وضع استعادة كلمة المرور. اكتب كلمة مرور جديدة.'); }
async function finishLogin(message='تم تسجيل الدخول بنجاح.'){ try { await MIS.saveProfile(); } catch(e) { console.warn('profile sync', e); } notice(message,true); setTimeout(()=>{location.href=destination;},500); }
async function refreshAuth(){ const user=await MIS.user(); if(user){ notice(`أنت مسجل الدخول: ${user.email||'حسابك'}`,true); if($('logout')) $('logout').hidden=false; } else { if(!$('recoveryBox')||$('recoveryBox').hidden) notice('أدخل بيانات حسابك لتسجيل الدخول.'); if($('logout')) $('logout').hidden=true; } }

// افتراضيًا الصفحة = تسجيل دخول فقط. إنشاء الحساب يفتح وضع التسجيل صراحة.
let authMode='login';
function setMode(mode){ authMode=mode; const nameField=$('fullName')?.closest('.field'); const login=$('login'); const signup=$('signup'); if(nameField) nameField.hidden=mode!=='signup'; if(login){login.hidden=mode==='signup';} if(signup){signup.hidden=mode!=='signup';} let toggle=$('authModeToggle'); if(!toggle){ toggle=document.createElement('button'); toggle.id='authModeToggle'; toggle.type='button'; toggle.className='btn secondary'; toggle.style.width='100%'; toggle.style.marginTop='12px'; $('loginArea')?.appendChild(toggle); toggle.addEventListener('click',()=>setMode(authMode==='login'?'signup':'login')); } toggle.textContent=mode==='login'?'🆕 إنشاء حساب جديد':'↩️ لدي حساب بالفعل — تسجيل الدخول'; const heading=document.querySelector('#loginArea')?.previousElementSibling?.querySelector('h1'); if(heading) heading.textContent=mode==='login'?'تسجيل الدخول إلى المنصة':'إنشاء حساب جديد على المنصة'; notice(mode==='login'?'لديك حساب بالفعل؟ أدخل البريد وكلمة المرور.':'أنشئ حسابك أولًا، وبعدها يمكنك تسجيل الدخول واستخدام المنصة.'); }

$('signup')?.addEventListener('click',async()=>{ if(authMode!=='signup') return setMode('signup'); const email=$('email').value.trim(),password=$('password').value,name=$('fullName').value.trim(); if(!email||!/^\S+@\S+\.\S+$/.test(email)) return notice('اكتب بريدًا إلكترونيًا صحيحًا.'); if(password.length<6) return notice('كلمة المرور يجب أن تكون 6 أحرف على الأقل.'); const {data,error}=await sb.auth.signUp({email,password,options:{data:{full_name:name}}}); if(error) return notice(error.message); if(data.user) await MIS.saveProfile(name); if(data.session) return finishLogin('تم إنشاء الحساب وتسجيل الدخول بنجاح ✓'); notice('تم إنشاء الحساب. أكّد بريدك الإلكتروني ثم ارجع إلى وضع تسجيل الدخول.'); setMode('login'); });
$('login')?.addEventListener('click',async()=>{ if(authMode!=='login') return setMode('login'); const email=$('email').value.trim(),password=$('password').value; if(!email||!password) return notice('اكتب البريد الإلكتروني وكلمة المرور.'); const {error}=await sb.auth.signInWithPassword({email,password}); if(error) return notice('بيانات الدخول غير صحيحة أو الحساب غير موجود.'); await finishLogin(); });
window.requestPasswordReset=async function(){const email=$('email')?.value.trim();if(!email||!/^\S+@\S+\.\S+$/.test(email))return notice('اكتب بريدك الإلكتروني أولًا، ثم اضغط «نسيت كلمة المرور؟».');const button=$('forgotPassword');if(button){button.disabled=true;button.textContent='⏳ جاري إرسال رابط الاستعادة...';}try{const {error}=await sb.auth.resetPasswordForEmail(email,{redirectTo:PRODUCTION_AUTH_URL});if(error)return notice(error.message);notice('تم إرسال رابط استعادة كلمة المرور إلى بريدك الإلكتروني.',true);}catch(error){notice(error?.message||'حدث خطأ أثناء طلب استعادة كلمة المرور.');}finally{if(button){button.disabled=false;button.textContent='🔑 نسيت كلمة المرور؟';}}};
$('forgotPassword')?.addEventListener('click',window.requestPasswordReset);
$('updatePassword')?.addEventListener('click',async()=>{const password=$('newPassword').value,confirm=$('confirmPassword').value;if(password.length<6)return notice('كلمة المرور الجديدة يجب أن تكون 6 أحرف على الأقل.');if(password!==confirm)return notice('كلمتا المرور غير متطابقتين.');const {error}=await sb.auth.updateUser({password});if(error)return notice(error.message);await finishLogin('تم تغيير كلمة المرور بنجاح ✓');});
async function social(provider){ if(authMode==='login'){ notice('لو دي أول مرة لك على المنصة، اضغط «إنشاء حساب جديد» أولًا ثم استخدم '+(provider==='google'?'Google':'Facebook')+' لإنشاء الحساب.'); return; } const redirectTo=`${PRODUCTION_AUTH_URL}?next=${encodeURIComponent(destination)}`; const {error}=await sb.auth.signInWithOAuth({provider,options:{redirectTo}}); if(error)notice(error.message); }
$('google')?.addEventListener('click',()=>social('google')); $('facebook')?.addEventListener('click',()=>social('facebook')); $('logout')?.addEventListener('click',async()=>{await sb.auth.signOut();location.href='auth.html';});
sb.auth.onAuthStateChange((event)=>{if(event==='PASSWORD_RECOVERY')showRecoveryMode();setTimeout(refreshAuth,0);});
(async()=>{const {data:{session}}=await sb.auth.getSession();if(session&&location.hash.includes('type=recovery'))showRecoveryMode();setMode('login');await refreshAuth();})();
