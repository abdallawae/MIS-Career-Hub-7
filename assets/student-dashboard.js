(()=>{const $=id=>document.getElementById(id);const names={sql:'SQL وقواعد البيانات',excel:'Excel الاحترافي','power-bi':'Power BI','systems-analysis':'تحليل وتصميم النظم',erp:'ERP',python:'Python وتحليل البيانات',web:'تطوير الويب',cybersecurity:'الأمن السيبراني',cloud:'الحوسبة السحابية','project-management':'إدارة المشروعات',career:'المسار الوظيفي','digital-transformation':'التحول الرقمي'};const esc=v=>String(v??'').replace(/[&<>\"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','\"':'&quot;',"'":'&#039;'}[c]));const pct=n=>Math.round(Math.min(60,n)/60*100);async function main(){const user=await MIS.user();if(!user){location.href='auth.html?next=student-dashboard.html';return}const profile=await MIS.profile();$('welcomeName').textContent=`أهلًا ${profile?.full_name||'يا بطل'} 👋`;$('welcomeText').textContent='كل تقدم تحققه هنا محفوظ على حسابك.';$('logout')?.addEventListener('click',async()=>{await sb.auth.signOut();location.href='auth.html'});const [enroll,prog,quiz,cert]=await Promise.all([sb.from('course_enrollments').select('course_slug,enrolled_at').eq('user_id',user.id).order('enrolled_at',{ascending:false}),sb.from('lesson_progress').select('course_slug,level_number,lesson_number,completed,completed_at').eq('user_id',user.id).eq('completed',true).order('completed_at',{ascending:false}),sb.from('quiz_results').select('course_slug,level_number,lesson_number,score,created_at').eq('user_id',user.id).order('created_at',{ascending:false}).limit(8),sb.from('certificates').select('certificate_id,course_slug,issued_at,status').eq('user_id',user.id).order('issued_at',{ascending:false})]);const E=enroll.data||[],P=prog.data||[],Q=quiz.data||[],C=cert.data||[];const by={};P.forEach(x=>(by[x.course_slug]??=[]).push(x));const total=P.length;const completedCourses=Object.values(by).filter(x=>x.length>=60).length;$('stats').innerHTML=[['📖',total,'درس مكتمل'],['🎯',Math.round(total/720*100)+'%','إجمالي الرحلة'],['🏆',C.filter(x=>x.status==='issued').length,'شهادة صادرة'],['🧪',Q.length,'اختبار محفوظ']].map(x=>`<div class="card"><div style="font-size:24px">${x[0]}</div><strong style="font-size:28px;display:block;margin:5px 0">${x[1]}</strong><span class="muted">${x[2]}</span></div>`).join('');const slugs=[...new Set([...E.map(x=>x.course_slug),...P.map(x=>x.course_slug)])];if(!slugs.length){$('tracks').innerHTML='<div class="empty muted">لسه مفيش مسار بدأت فيه. اختار مسار وابدأ أول درس 🚀</div>';return}$('tracks').innerHTML=slugs.map(slug=>{const n=by[slug]?.length||0;const last=by[slug]?.[0];const link=last?`courses/${slug}/lesson.html?level=${last.level_number}&lesson=${last.lesson_number}`:`courses/${slug}/index.html`;return `<div class="track-row"><div><strong>${esc(names[slug]||slug)}</strong><div class="muted2">${n}/60 درس</div></div><div><strong>${pct(n)}%</strong></div><div class="progress" style="color:currentColor"><i style="width:${pct(n)}%"></i></div><a class="btn small" href="${link}">${n?'متابعة':'ابدأ'}</a></div>`}).join('');$('trackMsg').textContent=`${slugs.length} مسار نشط • ${completedCourses} مكتمل`;$('quizzes').innerHTML=Q.length?Q.map(x=>`<a class="card" href="courses/${x.course_slug}/lesson.html?level=${x.level_number}&lesson=${x.lesson_number}"><strong>${esc(names[x.course_slug]||x.course_slug)}</strong><span class="muted2">المستوى ${x.level_number} • الدرس ${x.lesson_number} • النتيجة ${Number(x.score)||0}%</span></a>`).join(''):'<div class="empty muted">لم تحفظ أي اختبار بعد.</div>';$('certificates').innerHTML=C.filter(x=>x.status==='issued').length?C.filter(x=>x.status==='issued').map(x=>`<a class="card" href="certificate.html?id=${encodeURIComponent(x.certificate_id)}"><span class="badge">✓ صادرة</span><strong>${esc(names[x.course_slug]||x.course_slug)}</strong><span class="muted2">${esc(x.certificate_id)}</span></a>`).join(''):'<div class="empty muted">أكمل 60 درسًا في المسار لتحصل على الشهادة 🏆</div>'}document.addEventListener('DOMContentLoaded',main)})();