const $=id=>document.getElementById(id);const status=$('ownerStatus');function setStatus(t,ok=false){if(status){status.textContent=t;status.className='pill'+(ok?' success':'');}}function esc(v){return String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#039;'}[c]));}async function guard(){const user=await MIS.user();if(!user){setStatus('سجّل الدخول أولاً');location.href='auth.html?next=owner.html';return null;}const profile=await MIS.profile();if(profile?.role!=='admin'){setStatus('غير مصرح');document.querySelector('main').innerHTML='<div class="card" style="margin:40px auto;max-width:720px;text-align:center"><h1>⛔ الوصول للمالك فقط</h1><p class="muted">هذا القسم متاح للحساب الذي يحمل صلاحية Owner / Admin فقط.</p><a class="btn" href="index.html">العودة للموقع</a></div>';return null;}setStatus('Owner متصل ✓',true);return user;}async function count(table){const {count,error}=await sb.from(table).select('*',{count:'exact',head:true});if(error)throw error;return count||0;}async function load(){try{const user=await guard();if(!user)return;const [courses,students,progress,quizzes]=await Promise.all([count('courses'),count('profiles'),count('lesson_progress'),count('quiz_results')]);$('statCourses').textContent=courses;$('statStudents').textContent=students;$('statProgress').textContent=progress;$('statQuizzes').textContent=quizzes;const {data:courseData,error:ce}=await sb.from('courses').select('name,slug,is_published').order('created_at',{ascending:false});if(ce)throw ce;$('courseRows').innerHTML=(courseData||[]).map(c=>`<tr><td>${esc(c.name)}</td><td><code>${esc(c.slug)}</code></td><td>${c.is_published?'منشور':'مسودة'}</td><td><a href="course.html?course=${encodeURIComponent(c.slug)}">فتح</a></td></tr>`).join('')||'<tr><td colspan="4">لا توجد مسارات محفوظة بعد.</td></tr>';const {data:studentsData,error:se}=await sb.from('profiles').select('full_name,created_at').order('created_at',{ascending:false}).limit(20);if(se)throw se;$('studentRows').innerHTML=(studentsData||[]).map(s=>`<tr><td>${esc(s.full_name||'طالب')}</td><td>—</td><td>${s.created_at?new Date(s.created_at).toLocaleDateString('ar-EG'):'—'}</td></tr>`).join('')||'<tr><td colspan="3">لا يوجد طلاب بعد.</td></tr>';}catch(e){console.error(e);setStatus('تعذر تحميل البيانات');}}$('addCourse')?.addEventListener('click',async()=>{const name=$('newCourseName').value.trim(),slug=$('newCourseSlug').value.trim().toLowerCase();if(!name||!slug)return alert('اكتب اسم المسار والـ Slug.');const {error}=await sb.from('courses').insert({name,slug,is_published:true});if(error)return alert('تعذر إضافة المسار: '+error.message);$('newCourseName').value='';$('newCourseSlug').value='';load();});load();